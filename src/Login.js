import React, { useState } from "react";

const Login = () => {
  const [data, setData] = useState({});
  return (
    <div>
      <form>
        <input />
      </form>
    </div>
  );
};

export default Login;
